{
  "name": "entra-test",
  "version": "1.0.0",
  "private": true,
  "description": "A local mock of Microsoft Entra ID (OIDC/OAuth 2.0) plus a sample app, so apps can be developed and tested offline and then pointed at a real tenant unchanged.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/dev.js",
    "start:idp": "node mock-entra/server.js",
    "start:app": "node sample-app/server.js",
    "device-demo": "node examples/device-code-cli.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {}
}
